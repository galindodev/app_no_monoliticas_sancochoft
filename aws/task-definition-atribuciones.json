{
  "family": "atribuciones-task",
  "networkMode": "awsvpc",
  "requiresCompatibilities": ["FARGATE"],
  "cpu": "256",
  "memory": "512",
  "executionRoleArn": "arn:aws:iam::726264870413:role/ecsTaskExecutionRole",
  "taskRoleArn": "arn:aws:iam::726264870413:role/ecsTaskExecutionRole",
  "containerDefinitions": [
    {
      "name": "atribuciones",
      "image": "726264870413.dkr.ecr.us-east-1.amazonaws.com/atribuciones:latest",
      "portMappings": [
        {
          "containerPort": 5000,
          "protocol": "tcp"
        }
      ],
      "essential": true,
      "logConfiguration": {
        "logDriver": "awslogs",
        "options": {
          "awslogs-group": "/ecs/atribuciones",
          "awslogs-region": "us-east-1",
          "awslogs-stream-prefix": "ecs"
        }
      },
      "healthCheck": {
        "command": ["CMD-SHELL", "curl -f http://localhost:5000/health || exit 1"],
        "interval": 30,
        "timeout": 5,
        "retries": 3,
        "startPeriod": 60
      }
    }
  ]
}
